<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data> 

        <!-- Receipt dialog Form  -->
        <record id="view_vendor_receipt_dialog_form" model="ir.ui.view">
            <field name="name">account.voucher.receipt.dialog.receipt.inherit.form</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account_voucher.view_vendor_receipt_dialog_form"/>
            <field name="arch" type="xml">
                <notebook position="before">
                    <!-- notebook to be used by modules that inherit of this one -->
                    <notebook name="voucher_payline"/>
                </notebook>
                <form>
                    <field name="paylines_amount" invisible="1"/>
                </form>
                <!-- TODO check if we want to use this in some cases, to set a value -->
                <field name="amount" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <field name="journal_id" position="after">
                    <separator colspan="2"/>
                    <field name="reconciled_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <separator colspan="2"/>
                    <field name="net_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <label for="amount_readonly" string="Total Amount"/>
                    <div>
                        <field name="amount_readonly" class="oe_inline" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    </div>
                </field>
            </field>
        </record>

        <!-- Pay form  -->
        <record id="view_account_payment_form" model="ir.ui.view">
            <field name="name">account.payment.form</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form"/>
            <field name="arch" type="xml">
                <form>
                    <field name="paylines_amount" invisible="1"/>
                </form>
                <!-- TODO check if we want to use this in some cases, to set a value -->
                <field name="amount" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <field name="journal_id" position="after">
                    <separator colspan="2"/>
                    <field name="reconciled_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <separator colspan="2"/>
                    <field name="net_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <label for="amount_readonly" string="Total Amount"/>
                    <div>
                        <field name="amount_readonly" class="oe_inline" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    </div>
                </field>
            </field>
        </record>

        <!-- Pay from invoice  -->
        <record id="view_account_payment_invoice_form" model="ir.ui.view">
            <field name="name">account.payment.invoice.form</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_invoice_form"/>
            <field name="arch" type="xml">
                <form>
                    <field name="paylines_amount" invisible="1"/>
                </form>
                <!-- TODO check if we want to use this in some cases, to set a value -->
                <field name="amount" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <field name="journal_id" position="after">
                    <separator colspan="2"/>
                    <field name="reconciled_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <separator colspan="2"/>
                    <field name="net_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <label for="amount_readonly" string="Total Amount"/>
                    <div>
                        <field name="amount_readonly" class="oe_inline" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    </div>
                </field>
            </field>
        </record>
          
    </data>
</openerp>
