<?xml version="1.0" encoding="utf-8"?>
<!-- noupdate: an upgrade must not re-activate a cron someone deliberately
     stopped, nor reset the interval they tuned. -->
<odoo noupdate="1">

    <record forcecreate="True" id="ir_cron_background_send_payment_receipts" model="ir.cron">
        <field name="name">Background Send Payment Receipts: cron</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="state">code</field>
        <field name="code">model._cron_background_send_payment_receipts()</field>
        <field eval="True" name="active"/>
        <field name="interval_number">10</field>
        <field name="interval_type">minutes</field>
    </record>

</odoo>
